import { Card, Icon, Typography} from "@mui/material";
import React from "react";

export default function CareerLogo({logo, title, description}){
    return (
        <Card
            sx={{boxShadow: 0,
            borderRadius: 10,
            width:260}}
        >

        </Card>
    )
}